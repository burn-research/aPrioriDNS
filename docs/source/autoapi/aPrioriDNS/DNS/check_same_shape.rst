aPrioriDNS.DNS.check_same_shape
===============================

.. py:function:: aPrioriDNS.DNS.check_same_shape(*args)

   Checks if the shape of the input arguments *args is the same

   :returns: **bool** -- Assumes the value True only if all the inputs have the same shape.
   :rtype: bool

