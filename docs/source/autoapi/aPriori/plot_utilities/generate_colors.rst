aPriori.plot_utilities.generate_colors
======================================

.. py:function:: aPriori.plot_utilities.generate_colors(num_colors, cmap='plasma')

   Generates a list of distinct colors.

   :param num_colors: The number of colors to generate.
   :type num_colors: int

   :returns: A list of colors in hexadecimal format.
   :rtype: list

