aPriori.DNS.read_variable_in_chunks
===================================

.. py:function:: aPriori.DNS.read_variable_in_chunks(file_path, chunk_size)

   Generator to read a binary file in chunks.

   Parameters:
   -----------
   file_path : str
       Path to the binary file.
   chunk_size : int
       Number of elements to read per chunk.

   Returns:
   --------
   :
   np.ndarray
       Array of data read from the file.

